<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studenttoolkit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".notes"
            android:label="@string/title_activity_notes"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".Binary"></activity>
        <activity android:name=".Unit" />
        <activity android:name=".CGPA" />
        <activity android:name=".Temperature" />
        <activity android:name=".ConverterActivity" />
        <activity android:name=".help" />
        <activity android:name=".calculator" />
        <activity android:name=".splashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@drawable/circlebackgroundpurple" />

        <activity android:name=".MainActivity"/>

          <!--  <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
             -->
    </application>

</manifest>